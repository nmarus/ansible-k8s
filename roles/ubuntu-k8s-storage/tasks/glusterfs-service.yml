---
- name: Verify config directory /ansible_generated_configs
  become_method: sudo
  become: true
  file:
    path: /ansible_generated_configs
    state: directory

- name: Create /ansible_generated_configs/glusterfs-service.yaml
  become: true
  become_method: sudo
  template:
    src: glusterfs-service-yaml.j2
    dest: /ansible_generated_configs/glusterfs-service.yaml
    mode: 0644

- name: Fetch generated /ansible_generated_configs/glusterfs-service.yaml
  become_method: sudo
  become: true
  fetch:
    src: /ansible_generated_configs/glusterfs-service.yaml
    dest: "{{playbook_dir}}/generated"

- name: Create k8s gluster endpoints
  shell: kubectl apply -f /ansible_generated_configs/glusterfs-service.yaml

- name: Clean up temp files in /ansible_generated_configs
  become_method: sudo
  become: true
  file:
    path: /ansible_generated_configs
    state: absent
